import UiButton from "../components/UiButton";
import { Typography } from "@mui/material";

const Ui = () => {
  return (
    <div>
      <Typography variant="h1">測試</Typography>
      <UiButton variant="contained">Hello World</UiButton>
    </div>
  );
};

export default Ui;
